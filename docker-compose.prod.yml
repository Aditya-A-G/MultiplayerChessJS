version: '3'
services:
  nginx:
    ports:
      - "80:80"

  backend:
    build: 
      context: ./backend
      dockerfile: Dockerfile
      target: production    
    environment:
      - MONGO_USER=test
      - MONGO_PASSWORD=test
      - SESSION_SECRET=secret
      
    command: npm start